name: Supabase Keep Alive
on:
  schedule:
    - cron: '0 8 */3 * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping database analizzaste.it
        run: |
          CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://hvmnernovucnsksszsbe.supabase.co/rest/v1/reports?select=id&limit=1" -H "apikey: sb_publishable_5bBF51zEshWF3YtBh-YQoA_keV7WfCH")
          echo "Risposta: $CODE"
          [ "$CODE" = "200" ]
